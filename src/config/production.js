const API = {
  BASE_URL: 'https://wsedu.mayamd.ai',
  TOKEN_EXPIRE_TIME: '1800000', // time in milliseconds
  WEB_API_KEY: 'android'
};

const DEV_TOOLS = {
  logError: true
};

module.exports = {
  API,
  DEV_TOOLS
};
